void main() {
  int x = 30;
  int y = 20;

  final sum = x + y;
  final sub = x - y;
  final mul = x * y;
  final div = x / y;
  final divInInt = x ~/ y;
  final remainder = x % y;

  print(x > y);
  print(x < y);
  print(x >= y);
  print(x <= y);
  print(x == y);
  print(x != y);
  print(x > y);
  print(x > y);

  print(x == y && y == y);
  print(x == y || y == y);
}
